import 'package:flutter/material.dart';

class Consultas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: Text('Pagina consulta'),),
      body: new Column(
        children: <Widget>[
          new Text('Estamos en consultas')
        ],
      ),
    );
    
  }
}
