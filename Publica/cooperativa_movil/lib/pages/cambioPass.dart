import 'package:flutter/material.dart';

class CambioPass extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text('Update Pass'),
      ),
      body: new Column(
        children: <Widget>[new Text('Belcome Update Pass')],
      ),
    );
  }
}
