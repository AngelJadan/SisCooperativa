import 'package:flutter/material.dart';

class EstadoCuentas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text('Estado de Cuentas'),
      ),
      body: new Column(
        children: <Widget>[new Text('Page EstadoCuentas')],
      ),
    );
  }
}
