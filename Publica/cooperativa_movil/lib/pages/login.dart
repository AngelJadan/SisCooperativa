import 'package:flutter/material.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text('Pagina Login'),
      ),
      body: new Column(
        children: <Widget>[new Text('Page Login')],
      ),
    );
  }
}
